{% extends 'admin/base.html.twig' %}

{% set title = "Gestion du profil utilisateur" %}

{% block meta_title %}Admin - {{ title }}{% endblock %}

{% block body %}
<main class="mx-auto mt-16 w-full max-w-xl grow p-4">
    <h1 class="my-8 text-center text-4xl font">{{ title }}</h1>

    <div class="flex justify-end">
        <a class="rounded bg-slate-50 px-4 py-2 font-medium transition hover:bg-slate-600 hover:text-white"
           href="{{ path('app_admin_dashboard') }}">Retour</a>
    </div>

    {{ include('admin/user/_form.html.twig') }}
</main>
{% endblock %}
